{
  "name": "hydrants-app",
  "version": "1.0.0",
  "private": true,
  "description": "Hydrants inspection app — React + Express + SCSS (BEM) + Postgres",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:logs": "docker compose logs -f db",
    "db:bootstrap": "npm --prefix server run prisma:migrate -- --name init && npm --prefix server run seed",
    "prisma:migrate": "npm --prefix server run prisma:migrate",
    "prisma:studio": "npm --prefix server run prisma:studio",
    "seed": "npm --prefix server run seed",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
